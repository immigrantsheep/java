'use strict';

var PCR = require('./pcr');
var PCR_TOKEN = process.env.PCR_AUTH_TOKEN || 'public';
var pcr = new PCR(PCR_TOKEN);

var averageReviews = function averageReviews(courseAlias, cb) {
  pcr.courseHistoryReviews(courseAlias, function (err, json) {
    console.log(json.result);
  });
};

averageReviews('ASTR-150');